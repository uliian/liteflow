/**
 * Exceptions raised by the Rule-DB publisher API: configuration and provider resolution
 * failures, request validation failures, optimistic-version conflicts and backend storage
 * failures.
 */
package com.yomahub.liteflow.publisher.exception;
