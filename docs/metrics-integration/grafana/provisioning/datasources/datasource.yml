# 启动时自动把 Prometheus 配成 Grafana 数据源，无需手点
apiVersion: 1

datasources:
  - name: Prometheus
    uid: liteflow-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090     # 同一 docker compose 网络内用服务名访问
    isDefault: true
    editable: true
